<% if(errors.lenght > 0) { %>
<div class="row">
    <div class="col my-3">
      <div class="alert-danger">Um erro</div>
      <% errors.forEach(erro => { %>
        <%= erro %> <br>
      <% }); %>
    </div>
</div>
<% } %>